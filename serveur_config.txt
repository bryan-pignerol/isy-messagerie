# Configuration du Serveur ISY
# Ce fichier définit les paramètres réseau du serveur

# Adresse IP du serveur (utiliser 0.0.0.0 pour écouter sur toutes les interfaces)
IP=172.16.141.201

# Port d'écoute du serveur (par défaut: 8000)
PORT=8000

# Note: Les ports des groupes sont automatiquement assignés
# à partir de 8100 (PORT_GROUPE_BASE)